# @babel/plugin-syntax-xscript

> Allow parsing of the xscript syntax

## Install

Using npm:

```sh
npm install --save-dev @babel/plugin-syntax-xscript
```

or using yarn:

```sh
yarn add @babel/plugin-syntax-xscript --dev
```
