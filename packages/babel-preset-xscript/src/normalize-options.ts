import { OptionValidator } from "@babel/helper-validator-option";
const v = new OptionValidator("@babel/preset-xscript");

export default function normalizeOptions(options = {} as any) {

  return {

  };
}
